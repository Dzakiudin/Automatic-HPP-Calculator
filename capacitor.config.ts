import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hppkalkulator.ai',
  appName: 'Kalkulator HPP AI',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
